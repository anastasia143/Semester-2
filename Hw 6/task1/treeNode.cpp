#include "treeNode.h"
#include <iostream>

/*void TreeNode::print()
{

    if (node != NULL)
        {
            node->print();
            printPrefixTree(node->left);
            printPrefixTree(node->right);
        }
    }
}*/
